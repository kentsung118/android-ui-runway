<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    package="com.kent.android.slim.sample"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartAppActivity" />
        <activity android:name=".TransitionDrawableActivity" />
        <activity android:name=".ConstransLayoutDemoActivity" />
        <activity android:name=".letv.desktopmanager.DesktopManagerActivity" />
        <activity android:name=".workmanager.WorkActivity" />
        <activity android:name=".RoomActivity" />
        <activity android:name="com.android.ui.kent.demo.framwork.okhttp.OkHttpClientActivity" />
        <activity android:name=".share.ShareActivity" android:exported="true" />
        <activity android:name="com.android.ui.kent.demo.blank.AnimationActivityKt" />
    </application>

    <queries>
        <package android:name="com.instagram.android" />
        <package android:name="com.facebook.katana" />
    </queries>

</manifest>